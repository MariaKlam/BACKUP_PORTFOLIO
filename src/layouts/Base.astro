---
/* src/layouts/Base.astro */
const {
  title = "Maria Klammer – Portfolio",
  bodyClass = "",
  noWrap = false,        // render <slot/> without the centered .wrap
  hideFooter = false,    // hide footer for full-bleed demos
  mainClass = ""         // extra class on <main>
} = Astro.props;

const here = Astro.url.pathname;

// accepts a string or an array of prefixes
const isActive = (hrefs: string | string[]) => {
  const list = Array.isArray(hrefs) ? hrefs : [hrefs];
  return list.some((href) =>
    href === "/"
      ? here === "/"
      : here === href || here.startsWith(href + "/")
  );
};
---

<html lang="en">
  <head>

  <style is:inline>
    html {
      background: #ffffff;
    }

    html[data-theme="dark"] {
      background: #0b0c0f;
    }
  </style>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    

    <meta name="robots" content="noindex, nofollow">

    <!-- Instant theme to avoid flash -->
<script is:inline>
  (() => {
    try {
      const saved = localStorage.getItem("theme");

      const theme =
        saved === "dark" || saved === "light"
          ? saved
          : window.matchMedia("(prefers-color-scheme: dark)").matches
            ? "dark"
            : "light";

      document.documentElement.dataset.theme = theme;
      document.documentElement.style.colorScheme = theme;
    } catch {
      document.documentElement.dataset.theme = "light";
      document.documentElement.style.colorScheme = "light";
    }
  })();
</script>

    <link rel="stylesheet" href="/styles/main.css" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter+Tight:wght@400;600;800&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet">
<!-- in <head> -->

<style>

  :root { --ui-font: "Inter", system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, sans-serif; }
  body { font-family: var(--ui-font); }
.brand-icon {
  width: 32px;
  height: 32px;
  flex: 0 0 auto;
  display: block;
}

.brand-name {
  font-family: "Oswald", sans-serif;
  font-size: 15px;
  font-weight: 600;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  line-height: 1;

    font-family: "Oswald", "Arial Narrow", sans-serif;
    font-size: .95rem;
    font-weight: 700;
    letter-spacing: .12em;
}

.brand {
  display: inline-flex;
  align-items: center;
  gap: 10px;
}

.theme-toggle button {
  background: transparent;
  color: var(--muted);
}

html[data-theme="light"] .theme-toggle [data-theme-btn="light"],
html[data-theme="dark"] .theme-toggle [data-theme-btn="dark"] {
  background: var(--text);
  color: var(--bg);
}

.theme-toggle [data-theme-btn] {
  color: var(--text);
  opacity: .72;
}

html[data-theme="light"] .theme-toggle [data-theme-btn="light"],
html[data-theme="dark"] .theme-toggle [data-theme-btn="dark"] {
  opacity: 1;
}

html[data-theme="light"] .theme-toggle [data-theme-btn="light"] {
  background: #f2f2f2;
  color: #111;
}

html[data-theme="dark"] .theme-toggle [data-theme-btn="dark"] {
  background: #2a2b2f;
  color: #fff;
}

@media (max-width: 760px) {
  .brand-name {
    display: none;
  }

  .brand {
    gap: 0;
  }

  .brand-icon {
    width: 28px;
    height: 28px;
  }
}
</style>
<link href="https://fonts.googleapis.com/css2?family=Manrope:wght@500;600;700&family=Oswald:wght@500;600;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:wght@300..700" rel="stylesheet">
    <slot name="head" />
  </head>

 
  <body class={`menu__container has-thin-sidebar has-double-thin-sidebar menu--sidebar ${bodyClass}`}
      style="--card: var(--bg)">
    <header class="site-header">
  <div class="header-inner">
    <!-- Brand: icon + text (text visually hidden on mobile) -->
    <a class="brand" href="/" aria-label="Maria Klammer — Home">
<svg
  class="brand-icon"
  viewBox="0 0 32 32"
  xmlns="http://www.w3.org/2000/svg"
  aria-hidden="true"
>
  <circle cx="16" cy="16" r="8" fill="#FACC15" />

  <circle cx="13.2" cy="14.2" r="1.2" fill="#1A1A1A" />
  <circle cx="18.8" cy="14.2" r="1.2" fill="#1A1A1A" />

  <path
    d="M12.8 18c1.1 1.4 2.3 2.1 3.2 2.1s2.1-.7 3.2-2.1"
    stroke="#1A1A1A"
    stroke-width="1.6"
    stroke-linecap="round"
    fill="none"
  />
</svg>

  <span class="brand-name">MARIA KLAMMER</span>
</a>

    <nav class="nav">
      <a href="/work"    aria-current={isActive(["/work","/case-studies","/projects"]) ? "page" : undefined}>Work</a>
      <a href="/about"   aria-current={isActive("/about") ? "page" : undefined}>About</a>
      <a href="/contact" aria-current={isActive("/contact") ? "page" : undefined}>Contact</a>
    </nav>

    <!-- Desktop: large toggle -->
    <div class="theme-toggle" role="group" aria-label="Theme">
      <button type="button" data-theme-btn="light">Light</button>
      <button type="button" data-theme-btn="dark">Dark</button>
    </div>

    <!-- Mobile: mini icon toggle -->
    <button type="button" class="mini-theme-toggle" data-theme-toggle aria-label="Switch theme">
      <svg class="icon-sun"  viewBox="0 0 24 24" aria-hidden="true"><g fill="currentColor" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4.25"/><path d="M12 2.5v2.5M12 19v2.5M4.22 4.22l1.77 1.77M17.99 18l1.78 1.78M2.5 12H5M19 12h2.5M4.22 19.78L6 18M18 6l1.78-1.78"/></g></svg>
      <svg class="icon-moon" viewBox="0 0 24 24" aria-hidden="true"><path d="M21 13.25A8.75 8.75 0 0 1 10.75 3 9.75 9.75 0 1 0 21 13.25z" fill="currentColor" stroke="currentColor" stroke-width="1.75" stroke-linejoin="round"/></svg>
    </button>
  </div>
</header>

    <!-- main content slot -->
    { noWrap ? <slot /> : <main class="content"><div class="wrap"><slot /></div></main> }

    { !hideFooter &&
      <footer class="site-footer">
        <div class="bold-ticker" aria-label="Product, design and engineering capabilities">
          <div class="bold-ticker__rail">
            <div class="bold-ticker__track">
              <span>PRODUCT SYSTEMS</span><i>•</i>
              <span>UX/UI</span><i>•</i>
              <span>FRONTEND</span><i>•</i>
              <span>AI WORKFLOWS</span><i>•</i>
              <span>DESIGN SYSTEMS</span><i>•</i>
              <span>BRAND DIRECTION</span><i>•</i>
              <span>API INTEGRATIONS</span><i>•</i>
            </div>
            <div class="bold-ticker__track" aria-hidden="true">
              <span>PRODUCT SYSTEMS</span><i>•</i>
              <span>UX/UI</span><i>•</i>
              <span>FRONTEND</span><i>•</i>
              <span>AI WORKFLOWS</span><i>•</i>
              <span>DESIGN SYSTEMS</span><i>•</i>
              <span>BRAND DIRECTION</span><i>•</i>
              <span>API INTEGRATIONS</span><i>•</i>
            </div>
          </div>
        </div>
        <div class="footer-copy">© {new Date().getFullYear()} Maria Klammer — All rights reserved.</div>
      </footer>
    }

    <!-- Theme toggle wiring -->
<script>
  (function () {
    const KEY = "theme";
    const html = document.documentElement;

    function reflect(mode) {
      document.querySelectorAll("[data-theme-btn]").forEach((btn) => {
        const want = btn.getAttribute("data-theme-btn");

        btn.setAttribute(
          "aria-pressed",
          String(want === mode)
        );
      });

      document.querySelectorAll("[data-theme-toggle]").forEach((btn) => {
        btn.setAttribute(
          "aria-label",
          mode === "dark"
            ? "Switch to light theme"
            : "Switch to dark theme"
        );
      });
    }

    function setTheme(mode) {
      html.dataset.theme = mode;
      html.style.colorScheme = mode;

      reflect(mode);

      try {
        localStorage.setItem(KEY, mode);
      } catch {}
    }

    // theme already set in <head>
    reflect(html.dataset.theme === "dark" ? "dark" : "light");

    document.addEventListener("click", (e) => {
      const target = e.target;

      if (!(target instanceof Element)) return;

      /* DESKTOP LIGHT / DARK BUTTONS */
      const themeBtn = target.closest("[data-theme-btn]");

      if (themeBtn) {
        const mode = themeBtn.getAttribute("data-theme-btn");

        if (mode === "light" || mode === "dark") {
          setTheme(mode);
        }

        return;
      }

      /* MOBILE ICON BUTTON */
      const miniToggle = target.closest("[data-theme-toggle]");

      if (miniToggle) {
        const current =
          html.dataset.theme === "dark"
            ? "dark"
            : "light";

        setTheme(
          current === "dark"
            ? "light"
            : "dark"
        );
      }
    });

    const mq = window.matchMedia(
      "(prefers-color-scheme: dark)"
    );

    mq.addEventListener?.("change", (e) => {
      const saved = localStorage.getItem(KEY);

      if (saved !== "dark" && saved !== "light") {
        setTheme(e.matches ? "dark" : "light");
      }
    });
  })();
</script>


    <!-- Spotlight / reveal scripts (no-ops if no .cover-card present) -->
    <script is:inline>
      (function () {
        const cards = document.querySelectorAll('.cover-card');
        cards.forEach(el => {
          el.addEventListener('pointermove', e => {
            const r = el.getBoundingClientRect();
            el.style.setProperty('--mx', (e.clientX - r.left) + 'px');
            el.style.setProperty('--my', (e.clientY - r.top)  + 'px');
          });
          el.addEventListener('pointerleave', () => {
            el.style.removeProperty('--mx'); el.style.removeProperty('--my');
          });
        });
      })();
    </script>
    <script is:inline>
      (() => {
        const els = document.querySelectorAll('.cover-card');
        if (!('IntersectionObserver' in window) || !els.length) {
          els.forEach(el => el.classList.add('reveal-in'));
          return;
        }
        const io = new IntersectionObserver((entries) => {
          entries.forEach(e => {
            if (e.isIntersecting) {
              e.target.classList.add('reveal-in');
              io.unobserve(e.target);
            }
          });
        }, { rootMargin: '0px 0px -10% 0px', threshold: 0.05 });
        els.forEach(el => io.observe(el));
      })();
    </script>



    
  </body>
</html>
